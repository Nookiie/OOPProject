CREATE TABLE VIDEOGAMES (ID IDENTITY, GAME_NAME VARCHAR(40) NOT NULL, GAME_DESCRIPTION VARCHAR(120))
	ALTER TABLE VIDEOGAMES
	ADD COLUMN COMPANY_ID INT
	ADD COLUMN CATEGORY_ID INT
	
	ALTER TABLE VIDEOGAMES 
	ADD FOREIGN KEY (COMPANY_ID)
	REFERENCES COMPANIES (ID)
	
	ADD FOREIGN KEY (CATEGORY_ID)
	REFERENCES CATEGORIES (ID)
	

CREATE TABLE COMPANIES (ID IDENTITY, COMPANY_NAME VARCHAR(40) NOT NULL, COMPANY_DESCRIPTION VARCHAR(120))
CREATE TABLE CATEGORIES (ID IDENTITY, CATEGORY_NAME VARCHAR(40) NOT NULL)