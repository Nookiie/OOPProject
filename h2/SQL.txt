CREATE TABLE VIDEOGAMES (ID IDENTITY, NAME VARCHAR(40) NOT NULL, DESCRIPTION VARCHAR(120))
	ALTER TABLE VIDEOGAMES
	ADD COLUMN COMPANY_ID INT
	ADD COLUMN CATEGORY_ID INT
	
	ALTER TABLE VIDEOGAMES 
	ADD FOREIGN KEY (COMPANY_ID)
	REFERENCES COMPANIES (COMPANY_ID)
	
	ADD FOREIGN KEY (CATEGORY_ID)
	REFERENCES CATEGORIES (COMPANY_ID)
	

CREATE TABLE COMPANIES (ID IDENTITY, NAME VARCHAR(40) NOT NULL, DESCRIPTION VARCHAR(120))
CREATE TABLE CATEGORIES (ID IDENTITY, NAME VARCHAR(40) NOT NULL)